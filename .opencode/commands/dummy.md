---
description: Minimal test for parent-child session marker visibility
---

Spawn a sub-session with a simple prompt and verify the parent can see the response marker.

**Prompt for child session:**
Answer the following question in the following format: `<-- ANSWER "{{your-answer-text}}" -->` Only respond with the tag. The question is "How are you feeling today?"
