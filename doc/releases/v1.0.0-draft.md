# Demonlord v1.0.0 (Draft)

## Highlights
- Completed end-to-end factory validation for the Triage -> Implement -> Review lifecycle with deterministic orchestration integration tests.
- Expanded validation coverage for failure handling: network timeout, disk-full commit failures, and invalid orchestration snapshot configuration.
- Added broader simplified testing validation across framework combinations (Vitest/Jest + Playwright/Cypress) and snapshot auto-fix behavior.
- Re-validated Party Mode command flow and transcript export safety controls.

## Validation Evidence
- `.opencode` build/typecheck: pass (`npm run build`).
- Full `.opencode` test suite: pass (`npm run test -- --test-coverage`, 50/50 passing).
- Integration lifecycle tests: pass (`npm run test:integration`, 6/6 passing).
- Party Mode controls: pass (`node --test --experimental-strip-types tests/tools/party_mode.test.ts`, 4/4 passing).

## Release Checklist (Manual)
- [ ] Toggle repository "Template repository" setting in GitHub.
- [ ] Create and push release tag `v1.0.0` from the validated commit.
- [ ] Publish GitHub Release using this draft as the base notes.
